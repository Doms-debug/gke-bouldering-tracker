apiVersion: apps/v1
kind: Deployment
metadata:
  name: bouldering-tracker-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: bouldering-tracker
  template:
    metadata:
      labels:
        app: bouldering-tracker
    spec:
      containers:
      - name: api
        image: europe-central2-docker.pkg.dev/prj-cloud-sandbox-repo/bouldering-repo/bouldering-api:v1.0.0
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: "100m"    
            memory: "128Mi" 
          limits:
            cpu: "250m"     
            memory: "256Mi"